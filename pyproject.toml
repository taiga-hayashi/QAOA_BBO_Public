[project]
name = "qaoa-bbo"
version = "0.1.0"
description = "Factorization Machine + QAOA for Black-Box Optimization (BBO)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "qiskit",
    "qiskit-aer",
    "qiskit-algorithms",
    "qiskit-optimization",
    "matplotlib",
    "pyyaml",
]

[tool.uv]
package = false
